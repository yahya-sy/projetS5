{% extends 'base.html.twig' %}

{% block title %}Offreemploi{% endblock %}

{% block body %}
    <h1>Offreemploi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offreemploi.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offreemploi.description }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ offreemploi.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offreemploi_index') }}">back to list</a>

    <a href="{{ path('offreemploi_edit', {'id': offreemploi.id}) }}">edit</a>

    {{ include('offreemploi/_delete_form.html.twig') }}
{% endblock %}
