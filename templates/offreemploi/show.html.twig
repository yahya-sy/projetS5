{% extends 'base.html.twig' %}

{% block title %}Offreemploi{% endblock %}

{% block body %}
    <h1>Offreemploi</h1>

    <table class="table table-hover table-borderless">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offreemploi.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offreemploi.description }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ offreemploi.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('offreemploi_index') }}">back to list</a>
    <a class="btn btn-primary" href="{{ path('reponseoffre_new') }}">Postuler</a>

    {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-primary" href="{{ path('offreemploi_edit', {'id': offreemploi.id}) }}">Edit</a>
        {{ include('offreemploi/_delete_form.html.twig') }}
    {% endif %}

{% endblock %}
