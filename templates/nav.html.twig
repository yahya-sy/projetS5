<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="" href="/"><img class="" alt="Logo" src="/Images/Logo.png" width="50" height="50"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02"
                aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                <li>
                    <a class="nav-link" href="/">Accueil</a>
                </li>
                <li>
                    <a class="nav-link" href={{ path('article_index') }}>Nos Créations</a>
                </li>
                <li>
                    <a class="nav-link" href={{ path('offreemploi_index') }}>Rejoignez-nous</a>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <a class="nav-link text-white-50" href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a class="nav-link text-white-50" href="{{ path('app_login') }}">Connexion</a>
                    <a class="nav-link text-white-50" href="{{ path('app_register') }}">Créer un compte</a>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin') }}">Admin</a>
                    </li>
                {% endif %}

            </ul>
            <form class="d-flex">
                <label>
                    <input class="form-control me-sm-2" type="text" placeholder="Search">
                </label>
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>
