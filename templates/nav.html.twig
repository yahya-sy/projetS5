<nav>

    <ul class="nav-list">
        <li class="nav-item" id="Logo">
            <a href="/"><img alt="Logo" src="/Images/Logo.png" width="50" height="50"></a>
        </li>
        <li class="nav-item">
            <a href="/">Accueil</a>
        </li>
        <li class="nav-item">
            <a href={{ path('article_index') }}>Nos Créations</a>
        </li>
        <li class="nav-item">
            <a href={{ path('offreemploi_index') }}>Recrutement</a>
        </li>
        <li class="nav-item">
            <a href=#>Contact</a>
        </li>
                {% if is_granted('ROLE_USER') %}
                    <a class="nav-link text-white-50" href="{{ path('demandeprojet_index') }}">Demandes devis</a>
                    <a class="nav-link text-white-50" href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a class="nav-link text-white-50" href="{{ path('app_login') }}">Connexion</a>
                    <a class="nav-link text-white-50" href="{{ path('app_register') }}">Créer un compte</a>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin') }}">Admin</a>
                    </li>
                {% endif %}
    </ul>
</nav>
