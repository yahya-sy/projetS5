<form method="post" action="{{ path('pagedacceuil_delete', {'id': pagedacceuil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pagedacceuil.id) }}">
    <button class="btn btn-warning">Delete</button>
</form>
